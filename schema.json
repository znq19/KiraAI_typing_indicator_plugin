{
    "delay_seconds": {
        "name": "首次延时（秒）",
        "type": "number",
        "default": 2.0,
        "minimum": 0,
        "hint": "收到消息后延迟多少秒显示“正在输入...”。设为 0 则立即显示。"
    },
    "interval_seconds": {
        "name": "持续发送间隔（秒）",
        "type": "number",
        "default": 2,
        "minimum": 0.5,
        "hint": "首次显示后，每隔此时间重新发送一次“正在输入...”，直到回复发出。设为 0 则只发送一次。"
    }
}